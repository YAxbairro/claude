{
  "name": "fletcv",
  "version": "0.1.0",
  "private": true,
  "description": "FletCV — phone-based fleet management for Cabo Verde. Monorepo (core domain, API, driver app, manager dashboard).",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
